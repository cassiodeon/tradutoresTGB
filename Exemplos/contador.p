algoritmo contador;
variáveis
	contador : inteiro;
fim-variáveis

início
    contador := 0;

    enquanto contador < 10 faça
        imprima(contador , "\t");
        contador := contador + 1;
    fim-enquanto

fim